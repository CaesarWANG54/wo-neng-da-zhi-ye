# Security Policy

## 当前支持范围

《我能打职业》仍处于预发布原型阶段。安全修复只面向当前维护分支；历史快照、第三方分叉和未经批准的部署不在支持范围内。

当前工程是客户端静态应用，仓库中的Worker只提供静态托管与SPA回退：没有账号系统、支付流程、远程数据库或持久化业务后端。比赛与生涯存档保存在浏览器本地，客户端存档不是安全边界，也不承载真实货币价值。这个边界不代表可以在仓库、Issue、截图、存档或测试夹具中放入密码、访问令牌、个人信息或许可合同。

## 私下报告漏洞

请不要为未修复漏洞创建公开 Issue，也不要公开提交真实凭据或个人数据。

仓库建立后，维护者应先启用 GitHub 的 **Private vulnerability reporting**。报告者随后可从仓库的 **Security** 页面选择 **Report a vulnerability**，私下提交：

- 受影响的版本、提交或页面；
- 可重复的最小步骤与实际影响；
- 必要且已脱敏的日志、截图或概念验证；
- 已知的缓解办法。

如果该入口尚未启用，请只通过你与仓库所有者已经建立的私密沟通渠道询问报告方式，不要把漏洞细节放进公开 Issue。本项目尚未公布专用安全邮箱，因此本文不虚构联系人或地址。

## 密钥与隐私规则

- 不提交 `.env`、`.dev.vars`、私钥、证书、云端凭据、npm/GitHub令牌或签名材料。
- 示例配置只能使用明显无效的占位值，并保存为 `.env.example` 或 `.env.*.example`。
- 不把联盟、球队、球员、竞品、音频或其他许可合同及内部证据放入客户端或公开仓库。
- 测试与QA材料必须使用合成数据，不包含设备标识、网络地址、真实用户数据或本地绝对路径。
- 发现凭据进入Git历史时，应先撤销和轮换凭据，再按仓库所有者批准的历史清理流程处理；仅删除当前文件并不够。

## 构建与依赖安全

- 使用 `npm ci` 和已提交的 `package-lock.json`，不要在发布提交中混入未经审查的依赖升级。
- 每次提交都应通过移动运行时哈希锁、核心测试、浏览器测试、生产构建、内容扫描与Sites Worker测试。
- 音频只允许同源、清单登记且SHA-256匹配的文件；禁止以远程URL绕开内容门禁。
- Dependabot只能提交更新建议；合并前仍必须检查变更、许可证、构建产物和全套CI。

## 非安全问题

普通玩法缺陷、视觉问题和已脱敏的崩溃可使用常规Issue。权利许可、真人数据、品牌资产和第三方素材的发布资格由 `docs/rights/rights-ledger.md` 管理；它们不是通过公开安全报告渠道批准的。
